@page
@model ReadAcross.Pages_Reference.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Reference[0].Risdata)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Reference[0].FunctionalGroup)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Reference[0].Reaction)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Reference) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Risdata)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FunctionalGroup.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Reaction.Name)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
